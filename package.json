{
  "name": "auvergne-webcams",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "ajv": "^6.2.0",
    "material-ui": "^0.20.0",
    "react": "^16.2.0",
    "react-dom": "^16.2.1"
  },
  "devDependencies": {
    "react-scripts": "1.1.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build && cp -r proxy/ build/proxy",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject"
  }
}
